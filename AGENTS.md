# Agent Instructions

- Treat `DESIGN.md` as the source of truth for all UI changes.
- Reference `DESIGN.md` before editing Tailwind classes, component styling, layout, typography, colors, borders, radius, spacing, shadows, badges, tables, cards, buttons, or input states.
- Update Tailwind classes and theme tokens to match the Traceability Core design system instead of introducing ad hoc visual values.
- Preserve the system's intended visual language: light neutral surfaces, Deep Navy primary actions, Forest Green verification states, restrained borders, compact data density, Inter typography, and minimal shadows.
